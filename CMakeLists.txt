cmake_minimum_required(VERSION 3.20)
project(whats_an_nn)

add_subdirectory(cpp-scalar)
add_subdirectory(cpp-vector)
find_package(CUDAToolkit QUIET)
if(CUDAToolkit_FOUND)
    add_subdirectory(cpp-cuda)
else()
    message(STATUS "CUDA toolkit not found, skipping cpp-cuda target")
endif()